# AI Desktop Automation - Frontend

## How to Run (Windows)
1. Ensure Node.js and Rust are installed.
2. Run `npm install` to install dependencies.
3. Use the following command to start the desktop app:
   ```powershell
   npm run tauri dev